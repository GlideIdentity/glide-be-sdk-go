# Repository Template

This repository is used as the default template for new organization repositories.

It provides:
- A pre-initialized `master` branch
- Baseline repository structure
- Default organization policies

Do not develop directly in this repository.
